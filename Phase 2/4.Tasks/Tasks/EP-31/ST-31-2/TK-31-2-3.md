---
name: DevOps Task (AlertManager + Slack Webhook)
about: 0.4 PD
title: "[TK-31-2-3] Slack webhook 알림 룰 (AlertManager)"
labels: 'sprint:S0, epic:EP-31, story:ST-31-2, type:infra, priority:must, owner:devops, obs:alerting'
---

## :dart: Task Summary
- 추정: 0.4 PD / Owner: DevOps
- AlertManager 컨테이너 — Prometheus alert → Slack webhook. SRS OPS-003 정합 (Critical 60초 SLA). 첫 alert rule (probe failure) 검증.

## :link: References
- WBS §10 EP-31 ST-31-2 (Task 3)
- SRS OPS-003
- 연관: 선행 [TK-31-2-2](TK-31-2-2.md), [TK-31-1-2](../ST-31-1/TK-31-1-2.md), 후행 [TK-44-3-1](../../EP-44/ST-44-3/TK-44-3-1.md) (Phase 4 확장)

## :hammer_and_wrench: Implementation Plan

```
infra/docker-compose.yml                          [수정 — alertmanager service]
infra/observability/
  alertmanager.yml
  rules/probe-failure.yml                        [첫 alert rule]
```

### `docker-compose.yml`

```yaml
alertmanager:
  image: prom/alertmanager:v0.27.0
  volumes:
    - ./observability/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
  command:
    - '--config.file=/etc/alertmanager/alertmanager.yml'
    - '--storage.path=/alertmanager'
  ports: ["9093:9093"]
```

### `alertmanager.yml`

```yaml
global:
  resolve_timeout: 5m
  slack_api_url: ${SLACK_WEBHOOK_URL}

route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
  receiver: slack-default
  routes:
    - matchers: [severity="critical"]
      receiver: slack-critical
      group_wait: 0s
      repeat_interval: 15m

receivers:
  - name: slack-default
    slack_configs:
      - channel: '#scheduling-warnings'
        title: '⚠️ {{ .GroupLabels.alertname }}'
        text: '{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}'

  - name: slack-critical
    slack_configs:
      - channel: '#scheduling-alerts'
        title: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'
        text: '{{ range .Alerts }}{{ .Annotations.summary }}\n{{ .Annotations.runbook }}{{ end }}'
```

### `rules/probe-failure.yml` (sample alert)

```yaml
groups:
  - name: scheduling-probe
    rules:
      - alert: SchedulingEngineDown
        expr: up{job="scheduling-engine"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: 'scheduling-engine 다운 (1분 지속)'
          runbook: 'https://docs.internal/runbook/scheduling-down'
```

## :test_tube: Acceptance Criteria

- [ ] AlertManager 컨테이너 booting
- [ ] Slack webhook 발송 (test alert)
- [ ] Critical 라우팅 (0초 group_wait — 60초 SLA OPS-003)
- [ ] Warning 라우팅 (1분 group_wait — noise 절감)
- [ ] 첫 alert rule (probe failure) 발화 검증

## :checkered_flag: Definition of Done

- [ ] Compose + config + rules + 첫 alert
- [ ] Testcontainers — 의도적 down 시뮬 → Slack 발송 확인
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-31-2-2](TK-31-2-2.md), [TK-31-1-2](../ST-31-1/TK-31-1-2.md)
- 후행: [TK-44-3-1](../../EP-44/ST-44-3/TK-44-3-1.md), [TK-41-1-2](../../EP-41/ST-41-1/TK-41-1-2.md)
- Critical Path: ⭐⭐ (Phase 3 진입 게이트)

## :shield: Risk Mitigation

- Slack webhook URL — 환경 변수 (`SLACK_WEBHOOK_URL`) 분리, secret 노출 차단
- 60초 SLA — group_wait: 0s + 즉시 발송

## :memo: Implementation Notes

- Critical = page-worthy (밤·휴일 알림 OK), Warning = 영업시간만
- Phase 4 EP-44 ST-44-3에서 더 상세한 alert rules 추가
- 분기 인시던트 드릴 — 의도적 장애 발생 후 alert 발화 검증 (EP-44 ST-44-3-3)
