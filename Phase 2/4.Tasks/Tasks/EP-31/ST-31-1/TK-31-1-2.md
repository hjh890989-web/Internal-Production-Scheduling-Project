---
name: DevOps Task (Prometheus Scrape + Retention)
about: 0.7 PD
title: "[TK-31-1-2] Prometheus scrape + retention"
labels: 'sprint:S0, epic:EP-31, story:ST-31-1, type:infra, priority:must, owner:devops, obs:metrics'
---

## :dart: Task Summary
- 추정: 0.7 PD / Owner: DevOps
- Prometheus 2.x 컨테이너 — scheduling-engine `/actuator/prometheus` scrape. 30일 retention + persistent volume.

## :link: References
- WBS §10 EP-31 ST-31-1 (Task 2)
- SAD ADR-014
- 연관: 선행 [TK-31-1-1](TK-31-1-1.md), 후행 [TK-31-1-3](TK-31-1-3.md), [TK-40-1-3](../../EP-40/ST-40-1/TK-40-1-3.md)

## :hammer_and_wrench: Implementation Plan

```
infra/docker-compose.yml                          [수정 — prometheus service]
infra/observability/
  prometheus.yml
  rules/                                          [recording + alerting rules]
```

### `docker-compose.yml` prometheus service

```yaml
prometheus:
  image: prom/prometheus:v2.52.0
  command:
    - '--config.file=/etc/prometheus/prometheus.yml'
    - '--storage.tsdb.retention.time=30d'
    - '--web.enable-lifecycle'
  volumes:
    - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    - ./observability/rules:/etc/prometheus/rules:ro
    - prometheus-data:/prometheus
  ports: ["9090:9090"]
  depends_on: [scheduling-engine]
```

### `prometheus.yml`

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'scheduling-engine'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['scheduling-engine:8081']

  - job_name: 'keycloak'
    metrics_path: '/realms/master/metrics'    # Keycloak metrics endpoint
    static_configs:
      - targets: ['keycloak:8080']

  - job_name: 'postgres-exporter'
    static_configs:
      - targets: ['postgres-exporter:9187']

  - job_name: 'blackbox'
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - http://scheduling-engine:8080/actuator/health
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target

rule_files:
  - 'rules/*.yml'

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
```

## :test_tube: Acceptance Criteria

- [ ] Prometheus 컨테이너 booting
- [ ] scheduling-engine `/actuator/prometheus` scrape 정상 (15초)
- [ ] PostgreSQL exporter scrape
- [ ] Blackbox Exporter (`/health` probe)
- [ ] 30일 retention + persistent volume
- [ ] `/api/v1/status/config` reload 가능

## :checkered_flag: Definition of Done

- [ ] Compose + config + rules
- [ ] Testcontainers 통합 테스트
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-31-1-1](TK-31-1-1.md), [TK-00-1-1](../../EP-00/ST-00-1/TK-00-1-1.md)
- 후행: [TK-31-1-3](TK-31-1-3.md), [TK-41-1-1](../../EP-41/ST-41-1/TK-41-1-1.md) (Blackbox)
- Critical Path: ⭐⭐

## :memo: Implementation Notes

- 30일 retention — local storage (장기는 Loki·external object storage 향후)
- recording rules — EP-44 ST-44-2-3 KPI 집계 활용
- Multi-target dedup 검토 — 단일 인스턴스 환경에서는 불필요
