---
name: Infra Task (라우팅·상태관리)
about: 단일 작업 단위 — 0.4 PD 구현 명세
title: "[TK-00-3-3] React Router 6 + Zustand 4 + TanStack Query 5 골격"
labels: 'sprint:S0, epic:EP-00, story:ST-00-3, type:infra, priority:must, process:cross-cutting, owner:frontend'
assignees: ''
---

## :dart: Task Summary
- **Task ID**: TK-00-3-3
- **소속**: EP-00 인프라 기반 셋업 / ST-00-3 React + Vite 프론트엔드 골격 / Sprint S0 (Phase 0)
- **우선순위**: Must
- **추정**: 0.4 PD (~3.2 hours)
- **Owner role**: 프론트엔드
- **작업 요약**: TK-00-3-1·2 위에 **3가지 인프라**: (1) **React Router 6** 데이터 라우터(`createBrowserRouter`) — 코드 분할·lazy load 표준 패턴, (2) **Zustand 4** 클라이언트 상태 관리 (인증·UI prefs), (3) **TanStack Query 5** 서버 상태 캐싱 — 재시도·낙관적 업데이트·SWR. Sprint 1+ UI Story의 데이터 fetching·라우팅·상태 관리 표준 패턴 제공.

---

## :link: References
> :bulb: **AI Agent & Dev Note**: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.

- **상위 Story**: [`_Story_Overview.md`](_Story_Overview.md)
- **WBS Story 정의**: `Phase 2/4.Tasks/TASK-001_WBS_v1.2.md` §5.2 EP-00 ST-00-3 (Task 3 — 라우팅·상태관리)
- **SAD §5.2 프론트엔드 스택**:
  - 라우팅: **React Router 6.x** (표준)
  - 서버 상태: **TanStack Query 5.x** (캐싱·재시도·낙관적 업데이트)
  - 클라이언트 상태: **Zustand 4.x** (Redux 대비 boilerplate ↓)
- **SAD ADR-009**: 프론트엔드 스택 의사결정
- **SRS REQ-NF**:
  - **REQ-NF-PER-005** ("UI 페이지 응답 p95 ≤1초") → 라우트별 lazy load + TanStack Query 캐싱
  - REQ-NF-COM-001 (30명 동시 사용자) → Zustand 가벼움 + Query 캐싱
- **연관 Task**:
  - 선행: [TK-00-3-1](TK-00-3-1.md) (Vite + React), [TK-00-3-2](TK-00-3-2.md) (ConfigProvider 안에 위치)
  - 후행: Sprint 1+ 모든 UI Story (라우트·상태·fetch 표준)

---

## :hammer_and_wrench: Implementation Plan

### 변경 대상 (Files / Modules to Touch)
```
frontend/src/
├── main.tsx                            # QueryClientProvider 래핑
├── App.tsx                             # RouterProvider
├── router/
│   ├── index.tsx                       # createBrowserRouter + 라우트 정의
│   └── routes.ts                       # 라우트 상수
├── stores/
│   ├── authStore.ts                    # Zustand — 인증 정보
│   └── uiStore.ts                      # Zustand — UI prefs (사이드바·다크모드 등)
├── queries/
│   └── queryClient.ts                  # TanStack Query 설정
├── pages/
│   ├── HomePage.tsx                    # 표본 페이지
│   ├── NotFoundPage.tsx                # 404
│   └── layouts/
│       └── MainLayout.tsx              # 공통 레이아웃 (Header·Sider·Content)
├── api/
│   ├── client.ts                       # fetch wrapper (axios 또는 native fetch)
│   └── hooks/
│       └── useHealth.ts                # 표본 useQuery hook
```

### 핵심 로직 / 알고리즘

#### A. 의존성 추가

```json
// package.json 추가
{
  "dependencies": {
    "react-router-dom": "^6.27.0",
    "zustand": "^5.0.1",
    "@tanstack/react-query": "^5.59.16",
    "@tanstack/react-query-devtools": "^5.59.16"
  }
}
```

#### B. TanStack Query 설정 (`src/queries/queryClient.ts`)

```typescript
import { QueryClient } from '@tanstack/react-query'

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 60_000,        // 1분 fresh
      gcTime: 5 * 60_000,       // 5분 후 GC
      retry: (failureCount, error: any) => {
        // 4xx는 재시도 안 함 (서버 입력 검증 실패 등)
        if (error?.status >= 400 && error?.status < 500) return false
        return failureCount < 3
      },
      refetchOnWindowFocus: false,
    },
    mutations: {
      retry: false,             // mutation은 사용자 명시 재시도
    },
  },
})
```

#### C. Zustand store — 인증 (`src/stores/authStore.ts`)

```typescript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

interface AuthState {
  user: { id: string; name: string; role: 'planner' | 'floor_supervisor' | 'it_operator' | 'read_only' } | null
  token: string | null
  setUser: (user: AuthState['user'], token: string) => void
  logout: () => void
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      token: null,
      setUser: (user, token) => set({ user, token }),
      logout: () => set({ user: null, token: null }),
    }),
    {
      name: 'scheduling-auth',
      partialize: (state) => ({ user: state.user, token: state.token }),
    },
  ),
)
```

#### D. Zustand store — UI prefs (`src/stores/uiStore.ts`)

```typescript
import { create } from 'zustand'

interface UIState {
  sidebarCollapsed: boolean
  toggleSidebar: () => void
}

export const useUIStore = create<UIState>((set) => ({
  sidebarCollapsed: false,
  toggleSidebar: () => set((s) => ({ sidebarCollapsed: !s.sidebarCollapsed })),
}))
```

#### E. API client (`src/api/client.ts`)

```typescript
import { useAuthStore } from '@/stores/authStore'

class HttpError extends Error {
  constructor(public status: number, public body: unknown, message?: string) {
    super(message ?? `HTTP ${status}`)
  }
}

export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
  const token = useAuthStore.getState().token
  const headers = new Headers(init?.headers)
  headers.set('Content-Type', 'application/json')
  if (token) headers.set('Authorization', `Bearer ${token}`)

  const res = await fetch(path, { ...init, headers })
  if (!res.ok) {
    const body = await res.json().catch(() => null)
    throw new HttpError(res.status, body)
  }
  return (await res.json()) as T
}
```

#### F. 표본 useQuery hook (`src/api/hooks/useHealth.ts`)

```typescript
import { useQuery } from '@tanstack/react-query'
import { apiFetch } from '../client'

interface HealthResponse {
  status: 'UP' | 'DOWN'
  components: Record<string, { status: string }>
}

export function useHealth() {
  return useQuery({
    queryKey: ['actuator', 'health'],
    queryFn: () => apiFetch<HealthResponse>('/api/v1/actuator/health'),
    refetchInterval: 30_000,  // 30초마다 자동 갱신
  })
}
```

#### G. Router 정의 (`src/router/index.tsx`)

```tsx
import { createBrowserRouter, Navigate } from 'react-router-dom'
import { lazy, Suspense } from 'react'
import { Spin } from 'antd'
import MainLayout from '@/pages/layouts/MainLayout'

const HomePage     = lazy(() => import('@/pages/HomePage'))
const NotFoundPage = lazy(() => import('@/pages/NotFoundPage'))

const fallback = (
  <div style={{ display: 'flex', justifyContent: 'center', padding: 48 }}>
    <Spin size="large" />
  </div>
)

export const router = createBrowserRouter([
  {
    path: '/',
    element: <MainLayout />,
    errorElement: <NotFoundPage />,
    children: [
      { index: true, element: <Navigate to="/home" replace /> },
      {
        path: 'home',
        element: <Suspense fallback={fallback}><HomePage /></Suspense>,
      },
      // Sprint 1+ UI Story에서 추가 (예: /orders, /vc, /ex, /master, /audit)
      { path: '*', element: <NotFoundPage /> },
    ],
  },
])
```

#### H. MainLayout (`src/pages/layouts/MainLayout.tsx`)

```tsx
import { Layout, Menu, Typography } from 'antd'
import { Outlet, useNavigate, useLocation } from 'react-router-dom'
import { useTranslation } from 'react-i18next'
import { useUIStore } from '@/stores/uiStore'

const { Header, Sider, Content } = Layout
const { Title } = Typography

export default function MainLayout() {
  const { t } = useTranslation()
  const navigate = useNavigate()
  const location = useLocation()
  const collapsed = useUIStore((s) => s.sidebarCollapsed)

  const menuItems = [
    { key: '/home',    label: t('menu.home') },
    { key: '/orders',  label: t('menu.orders'),  disabled: true },  // Sprint 1+
    { key: '/vc',      label: t('menu.vc'),      disabled: true },
    { key: '/ex',      label: t('menu.ex'),      disabled: true },
    { key: '/master',  label: t('menu.master'),  disabled: true },
    { key: '/audit',   label: t('menu.audit'),   disabled: true },
  ]

  return (
    <Layout style={{ minHeight: '100vh' }}>
      <Header style={{ display: 'flex', alignItems: 'center' }}>
        <Title level={4} style={{ color: '#fff', margin: 0 }}>
          {t('app.title')}
        </Title>
      </Header>
      <Layout>
        <Sider collapsed={collapsed} width={220} theme="light">
          <Menu
            mode="inline"
            selectedKeys={[location.pathname]}
            onClick={(e) => navigate(e.key)}
            items={menuItems}
          />
        </Sider>
        <Content style={{ padding: 24 }}>
          <Outlet />
        </Content>
      </Layout>
    </Layout>
  )
}
```

#### I. HomePage 표본 (`src/pages/HomePage.tsx`)

```tsx
import { Card, Space, Tag, Typography } from 'antd'
import { useTranslation } from 'react-i18next'
import { useHealth } from '@/api/hooks/useHealth'
import { useAuthStore } from '@/stores/authStore'

const { Paragraph } = Typography

export default function HomePage() {
  const { t } = useTranslation()
  const { data, isLoading, error } = useHealth()
  const user = useAuthStore((s) => s.user)

  return (
    <Space direction="vertical" size="middle" style={{ display: 'flex' }}>
      <Card title={t('app.title')}>
        <Paragraph>Phase 2/4.Tasks ST-00-3 골격 — Sprint 1+에서 본 화면 대체.</Paragraph>
        {user && <Paragraph>로그인: {user.name} ({user.role})</Paragraph>}
      </Card>
      <Card title="Backend Health (Actuator)">
        {isLoading && t('common.loading')}
        {error && <Tag color="error">{t('common.error')}</Tag>}
        {data && <Tag color={data.status === 'UP' ? 'success' : 'error'}>{data.status}</Tag>}
      </Card>
    </Space>
  )
}
```

#### J. main.tsx 통합

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { ConfigProvider } from 'antd'
import { QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'
import { RouterProvider } from 'react-router-dom'
import koKR from 'antd/locale/ko_KR'
import dayjs from 'dayjs'
import 'dayjs/locale/ko'
import { router } from './router'
import { queryClient } from './queries/queryClient'
import { theme } from './theme'
import './i18n'
import './styles/global.css'

dayjs.locale('ko')

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider locale={koKR} theme={theme}>
      <QueryClientProvider client={queryClient}>
        <RouterProvider router={router} />
        {import.meta.env.DEV && <ReactQueryDevtools />}
      </QueryClientProvider>
    </ConfigProvider>
  </React.StrictMode>,
)
```

---

## :test_tube: Acceptance Criteria

**1차 검증 방법** (SRS §4.1.6 카탈로그):
- **1차**: T-U (Vitest — Zustand store + Query hook 단위 테스트)
- **2차**: T-I (통합 — 라우팅 + state 변경 + mock fetch 시나리오)

### 측정 가능 기준
- [ ] `npm install` 후 react-router-dom·zustand·@tanstack/react-query 정상 설치
- [ ] `npm run build` 성공 (router·query·zustand 청크 분리 ≤200 KB gzip 추가)
- [ ] `/` 진입 시 `/home`로 redirect → MainLayout + HomePage 렌더링
- [ ] `/존재하지않는경로` 진입 시 NotFoundPage 렌더링
- [ ] HomePage의 `useHealth` 가 `/api/v1/actuator/health` 호출 (MSW 또는 Mock으로 검증)
- [ ] Zustand `useAuthStore.setUser()` 호출 후 상태 변경 → HomePage 사용자 정보 표시
- [ ] localStorage에 `scheduling-auth` 키 저장 (persist 미들웨어)
- [ ] React Query Devtools가 DEV 모드에서만 보임 (`import.meta.env.DEV` 가드)
- [ ] 라우트 lazy load 동작 — `dist/assets/HomePage-*.js` 별도 청크
- [ ] 사이드바 메뉴 한국어 (i18n 통합) — `메뉴` 라벨 한글 표시

### 회귀 시나리오 (T-U + T-I)
- **케이스 1 (라우팅)**: `/` → `/home` redirect → MainLayout 진입
- **케이스 2 (404)**: `/foo` → NotFoundPage
- **케이스 3 (Zustand)**: setUser 호출 → HomePage user 표시 → reload → localStorage에서 복원
- **케이스 4 (Query)**: MSW로 `/api/v1/actuator/health` mock → useHealth가 `UP` 태그 표시
- **케이스 5 (lazy load)**: HomePage 첫 진입 시 Spin loader → 청크 로드 후 페이지 표시

---

## :checkered_flag: Definition of Done
- [ ] 위 모든 측정 기준 통과
- [ ] 3 인프라 (Router·Zustand·Query) 모두 셋업 완료
- [ ] MainLayout + HomePage + NotFoundPage 표본 페이지 3개 작성
- [ ] API client (`apiFetch`) wrapper + 표본 hook (`useHealth`)
- [ ] 코드 리뷰 1명 이상 승인 (프론트 리드)
- [ ] NFR 매핑: REQ-NF-PER-005 (lazy load) + REQ-NF-USA-003 (메뉴 한국어)
- [ ] **ST-00-3 Story 레벨 DoD 완결** — `docker compose up`으로 한국어 SPA + 사이드바 + Backend health 통신 시연 가능
- [ ] Sprint Review 데모: SPA 진입 → 메뉴 클릭 → React Router 동작 → Zustand state → Query mock fetch

---

## :construction: Dependencies
- **선행 (Depends on)**:
  - 같은 Story 내: [TK-00-3-1](TK-00-3-1.md) (Vite + React 골격), [TK-00-3-2](TK-00-3-2.md) (Ant Design + i18n)
- **후행 (Blocks)**:
  - Sprint 1+ 모든 UI Story (EP-04 ST-04-3·EP-07·EP-15·EP-17·EP-18·EP-19 등) — 본 Task의 Router·Zustand·Query 패턴 위에 페이지 추가
  - EP-30 ST-30-2 (Keycloak SSO 통합 — useAuthStore와 결합)
- **Critical Path 여부**: ⭐ **Critical Path 속함** (UI Sprint 진입 표준 패턴)
- **Phase B 종속**: 해당 없음

---

## :shield: Risk Mitigation
- **잔여 위험·전제**:
  - 전제 1: TanStack Query v5는 v4와 API 호환성 차이 — `cacheTime` → `gcTime` 등 명명 변경
  - 전제 2: React Router 6.4+ 데이터 라우터 (`createBrowserRouter`) 사용 — 5.x 또는 6.0 비호환
  - 잔여 위험: Zustand persist 미들웨어가 SSR 미지원 — 본 프로젝트는 CSR-only이므로 영향 없음

---

## :memo: Implementation Notes
- **기술 힌트**:
  - `createBrowserRouter` + `RouterProvider` 패턴 = React Router 6.4+ 권장 (이전 `<BrowserRouter>` + `<Routes>` 대체)
  - Zustand `persist` 미들웨어 → localStorage 자동 동기화, `partialize`로 민감 데이터 제외 가능
  - TanStack Query v5의 `gcTime` (구 cacheTime) — 컴포넌트 unmount 후 GC 대기 시간
  - `import.meta.env.DEV` 가드로 ReactQueryDevtools를 PROD 빌드에서 자동 제거 (tree shaking)
- **유사 패턴**:
  - Sprint 1+ UI Story에서 새 페이지 추가 시: `pages/<Name>Page.tsx` + `router/index.tsx`에 `lazy()` 라우트 1개 추가
  - 새 API 호출 시: `api/hooks/use<Name>.ts` + TanStack Query useQuery/useMutation 1개
- **표준 참조**:
  - React Router 6 Tutorial: https://reactrouter.com/en/main/start/tutorial
  - Zustand: https://zustand-demo.pmnd.rs/
  - TanStack Query: https://tanstack.com/query/latest/docs/framework/react/overview
- **참고 외부 자료**:
  - Sprint 1+ UI 개발자 onboarding 시 위 3 라이브러리 학습 자료 (각 1시간 분량) 권장
