---
name: Infra Task (프론트엔드 골격)
about: 단일 작업 단위 — 0.5 PD 구현 명세
title: "[TK-00-3-1] Vite 5 + TypeScript 5.4 + React 18.3 프로젝트 + Dockerfile"
labels: 'sprint:S0, epic:EP-00, story:ST-00-3, type:infra, priority:must, process:cross-cutting, owner:frontend'
assignees: ''
---

## :dart: Task Summary
- **Task ID**: TK-00-3-1
- **소속**: EP-00 인프라 기반 셋업 / ST-00-3 React + Vite 프론트엔드 골격 / Sprint S0 (Phase 0)
- **우선순위**: Must
- **추정**: 0.5 PD (~4 hours)
- **Owner role**: 프론트엔드 (Vite + React + TS 운영 경험)
- **작업 요약**: **Vite 5 + TypeScript 5.4 + React 18.3** SPA 프로젝트를 부트스트랩. TypeScript strict 모드 + ESLint + Prettier + Vitest 셋업. `npm run build` 산출물(`dist/`)을 ST-00-1 NGINX 정적 파일 볼륨에 마운트. Dockerfile 멀티 스테이지 빌드(빌더: Node, 산출물 복사용 alpine).

---

## :link: References
> :bulb: **AI Agent & Dev Note**: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.

- **상위 Story**: [`_Story_Overview.md`](_Story_Overview.md)
- **WBS Story 정의**: `Phase 2/4.Tasks/TASK-001_WBS_v1.2.md` §5.2 EP-00 ST-00-3 (Task 1)
- **SAD ADR**:
  - **ADR-009** (TypeScript 5.4 + React 18.3 + Vite 5 + Ant Design 5)
- **SAD §5.2 프론트엔드 스택** (line 345~357): 본 Task는 빌드·언어·테스트 부분 (UI/i18n/Router는 TK-00-3-2·3)
- **SRS REQ-NF**:
  - **REQ-NF-PER-005** ("UI 페이지 응답 p95 ≤1초") → Vite code splitting + lazy load + tree shaking
  - **REQ-NF-COM-005** ("최신 2개 메이저 Chromium 기반") → `browserslist` Chromium 마지막 2 메이저
  - **REQ-NF-USA-004** ("플래너 ≥1280×800, 현장 패드 ≥1024×768") → viewport meta 설정
- **연관 Task**:
  - 같은 Story 내 후행: [TK-00-3-2](TK-00-3-2.md) (Ant Design + i18n), [TK-00-3-3](TK-00-3-3.md) (Router + Zustand)
  - 다른 Story: [ST-00-1 TK-00-1-3](../ST-00-1/TK-00-1-3.md) (NGINX `frontend-static` 볼륨 — 본 Task의 `dist/` 산출물 마운트 대상)

---

## :hammer_and_wrench: Implementation Plan

### 변경 대상 (Files / Modules to Touch)
```
frontend/
├── package.json
├── pnpm-lock.yaml                     # 또는 package-lock.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── .eslintrc.cjs
├── .prettierrc.json
├── .gitignore                         # node_modules, dist
├── index.html
├── public/
│   └── favicon.ico
├── src/
│   ├── main.tsx                       # entry point
│   ├── App.tsx                        # 루트 컴포넌트
│   ├── App.test.tsx                   # Vitest 표본
│   ├── vite-env.d.ts
│   └── assets/
└── Dockerfile                         # 멀티 스테이지 빌드
```

### 핵심 로직 / 알고리즘

#### A. `package.json`

```json
{
  "name": "scheduling-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "format": "prettier --write src/**/*.{ts,tsx,css,json}",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.4.5",
    "vite": "^5.4.10",
    "vitest": "^2.1.4",
    "@testing-library/react": "^16.0.1",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/user-event": "^14.5.2",
    "jsdom": "^25.0.1",
    "eslint": "^9.13.0",
    "@typescript-eslint/eslint-plugin": "^8.12.0",
    "@typescript-eslint/parser": "^8.12.0",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14",
    "prettier": "^3.3.3"
  }
}
```

#### B. `tsconfig.json` (strict 모드)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true,
    "noUncheckedIndexedAccess": true,

    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

#### C. `vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    host: '127.0.0.1',           // DEV: localhost only
    proxy: {
      '/api':  { target: 'http://localhost:8080', changeOrigin: true },
      '/ws':   { target: 'ws://localhost:8080', ws: true },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
    target: 'es2022',
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'ant-design':   ['antd'],
        },
      },
    },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test-setup.ts',
  },
})
```

#### D. `index.html` + viewport (NFR-USA-004)

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0" />
    <title>사내 공정 스케줄링 시스템</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

#### E. `src/main.tsx` + `App.tsx` 골격

```tsx
// src/main.tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

```tsx
// src/App.tsx
function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1>사내 공정 스케줄링 시스템</h1>
      <p>Phase 2/4.Tasks ST-00-3 골격 — Sprint 1+에서 본 화면을 대체합니다.</p>
    </div>
  )
}

export default App
```

#### F. `App.test.tsx` 표본 단위 테스트

```tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import App from './App'

describe('App', () => {
  it('renders 한국어 제목', () => {
    render(<App />)
    expect(screen.getByText('사내 공정 스케줄링 시스템')).toBeInTheDocument()
  })
})
```

#### G. ESLint·Prettier 설정 (간략)

```javascript
// .eslintrc.cjs
module.exports = {
  root: true,
  env: { browser: true, es2022: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
}
```

```json
// .prettierrc.json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100
}
```

#### H. Dockerfile 멀티 스테이지

```dockerfile
# frontend/Dockerfile
# 빌더
FROM node:20-alpine AS builder
WORKDIR /workspace
COPY package.json pnpm-lock.yaml* package-lock.json* ./
RUN npm ci || npm install
COPY . .
RUN npm run build

# 산출물 + NGINX serve용
FROM nginx:1.25-alpine
COPY --from=builder /workspace/dist /usr/share/nginx/html
# Healthcheck는 ST-00-1의 NGINX 컨테이너가 담당
```

---

## :test_tube: Acceptance Criteria

**1차 검증 방법** (SRS §4.1.6 카탈로그):
- **1차**: T-U (Build + Vitest 단위 테스트)
- **2차**: I (Inspection — tsconfig·vite.config·package.json 검토)

### 측정 가능 기준
- [ ] `npm install` 성공 (lock 파일 commit)
- [ ] `npm run build` 성공 + `dist/` 폴더 생성 + 빌드 시간 ≤30초
- [ ] `npm run test:run` Vitest 표본 1개 PASS
- [ ] `npm run lint` 경고 0건
- [ ] TypeScript strict 모드 ON + `noUnusedLocals`·`noUncheckedIndexedAccess` 활성
- [ ] `index.html` `lang="ko"` + viewport meta 설정
- [ ] `vite.config.ts` proxy 설정 (DEV에서 `/api`·`/ws` → Backend 8080)
- [ ] `build.rollupOptions.output.manualChunks`로 react·antd 청크 분리 (TK-00-3-2에서 antd 추가 시 적용)
- [ ] Dockerfile 멀티 스테이지 빌드 → 최종 이미지 ≤50 MB (alpine + dist만)
- [ ] **빌드 산출물이 ST-00-1 `frontend-static` 볼륨에 마운트되어 NGINX → SPA 진입 200 OK**

### 회귀 시나리오 (T-U)
- **케이스 1 (정상 빌드)**: clean → `npm install` → `npm run build` → BUILD SUCCESS ≤30초
- **케이스 2 (strict 모드)**: 의도적 `noUnusedLocals` 위반 → 빌드 실패
- **케이스 3 (test)**: `App.test.tsx` PASS, 한국어 텍스트 매칭
- **케이스 4 (Dockerfile)**: `docker build -t scheduling-frontend .` 성공, 이미지 크기 검증

---

## :checkered_flag: Definition of Done
- [ ] 위 모든 측정 기준 통과
- [ ] `package.json` lock 파일 git 추적
- [ ] `.gitignore`에 `frontend/node_modules`·`frontend/dist` 추가
- [ ] Dockerfile 멀티 스테이지 + ST-00-1 compose의 frontend 서비스 교체
- [ ] 코드 리뷰 1명 이상 승인 (프론트 리드)
- [ ] NFR 매핑: REQ-NF-PER-005·USA-004·COM-005
- [ ] Sprint Review 데모: `npm run build` + `docker compose up` → NGINX → 한국어 SPA 진입 시연

---

## :construction: Dependencies
- **선행 (Depends on)**:
  - 같은 Epic 내: [ST-00-1 TK-00-1-3](../ST-00-1/TK-00-1-3.md) (NGINX — SPA fallback `try_files`)
- **후행 (Blocks)**:
  - 같은 Story 내: [TK-00-3-2](TK-00-3-2.md) (Ant Design + i18n), [TK-00-3-3](TK-00-3-3.md) (Router + Zustand)
  - Sprint 2~5 모든 UI Story (EP-04 ST-04-3·EP-07·EP-15·EP-17 등) — 본 골격 위에 구현
- **Critical Path 여부**: ⭐ **Critical Path 속함** (모든 UI Sprint 진입 게이트)
- **Phase B 종속**: 해당 없음

---

## :shield: Risk Mitigation
- **잔여 위험·전제**:
  - 전제 1: Node 20 LTS 호스트에서 빌드 — 다른 Node 버전 사용 시 `package.json` engines 명시 권장
  - 전제 2: TypeScript strict 모드는 학습 곡선 있음 — 신규 개발자 onboarding 가이드 (Sprint 1 시작 전 추가) 필요
  - 잔여 위험: Vite 5의 plugin 호환성 (예: `@vitejs/plugin-react` 4.x 동기) — 의존성 lock 파일 commit으로 reproducibility 보장

---

## :memo: Implementation Notes
- **기술 힌트**:
  - Vite 5 + React 18 + TS 5 표준 stack — 2025 기준 가장 안정적인 SPA 빌드 조합
  - `manualChunks`로 react-vendor·ant-design 분리 → 캐시 효율 ↑ (NFR-PER-005)
  - `noUncheckedIndexedAccess: true` — 배열 인덱싱 시 `T | undefined` 강제 → 런타임 에러 ↓
- **유사 패턴**:
  - 향후 Phase 2 신규 SPA 모듈 추가 시 동일 패턴 (`apps/<module>/` 멀티 패키지 가능)
- **표준 참조**:
  - Vite Docs: https://vitejs.dev/guide/
  - React 18 Strict Mode: https://react.dev/reference/react/StrictMode
  - TypeScript strict: https://www.typescriptlang.org/tsconfig#strict
- **참고 외부 자료**:
  - Vite + React + TS 템플릿: https://vitejs.dev/guide/#scaffolding-your-first-vite-project
