---
name: Infra Task (모듈 경계 빌드타임 강제)
about: 단일 작업 단위 — 0.4 PD 구현 명세
title: "[TK-00-2-3] ArchUnit 모듈 경계 테스트 + Spring Modulith 검증 통합"
labels: 'sprint:S0, epic:EP-00, story:ST-00-2, type:infra, priority:must, process:cross-cutting, owner:backend'
assignees: ''
---

## :dart: Task Summary
- **Task ID**: TK-00-2-3
- **소속**: EP-00 인프라 기반 셋업 / ST-00-2 Spring Boot 모듈러 모놀리식 골격 / Sprint S0 (Phase 0)
- **우선순위**: Must
- **추정**: 0.4 PD (~3.2 hours)
- **Owner role**: 백엔드 + QA (ArchUnit 테스트 작성 + CI/CD 게이트 결합)
- **작업 요약**: TK-00-2-2의 Spring Modulith verify는 런타임 단계 검증 — **빌드 타임에 강제하려면 ArchUnit**(SAD §4.1·§5.1 명시). ArchUnit 5.x JUnit 5 통합으로 (1) 모듈 경계 위반 0건 단위 테스트, (2) 의존성 그래프 순환 금지, (3) Layered Architecture 규칙 (Web → Service → Repository), (4) 명명 규칙 검증 (`*Controller`·`*Service`·`*Repository`). 본 Task가 통과해야 **Sprint 0 DoD 항목 2번 "Spring Modulith ArchUnit 테스트 통과 (모듈 경계 위반 0)"** 직접 달성.

---

## :link: References
> :bulb: **AI Agent & Dev Note**: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.

- **상위 Story**: [`_Story_Overview.md`](_Story_Overview.md)
- **WBS Story 정의**: `Phase 2/4.Tasks/TASK-001_WBS_v1.2.md` §5.2 EP-00 ST-00-2 (Task 3) + §3 Sprint 0 DoD 항목 2 ("Spring Modulith ArchUnit 테스트 통과 — 모듈 경계 위반 0")
- **SAD ADR / §**:
  - **ADR-008** (Spring Modulith + ArchUnit으로 모듈 경계 강제)
  - SAD §4.1 모듈 경계 원칙 (line 312): "**Spring Modulith 또는 ArchUnit 테스트**"
  - SAD §5.1 (line 337): "**Spring Modulith** | 1.2.x | 모듈 경계 위반을 **빌드 타임에 차단**"
  - SAD §12 추적성 (line 1038): "Spring Modulith + ArchUnit | NFR-COM-004 (API 전방 호환성 확장 안전성)"
- **SRS REQ-NF**:
  - **REQ-NF-COM-004** (API 전방 호환성 — Phase 2 MRP 결합 가능) → ArchUnit이 모듈 경계 회귀 방지
- **연관 Task**:
  - 선행: [TK-00-2-1](TK-00-2-1.md) (Gradle subproject + ArchUnit BOM), [TK-00-2-2](TK-00-2-2.md) (Modulith 모듈 정의)
  - 후행: EP-32 ST-32-1 (CI/CD — 본 Task의 테스트를 Jenkins 파이프라인 게이트로 적용)

---

## :hammer_and_wrench: Implementation Plan

### 변경 대상 (Files / Modules to Touch)
- `backend/app/src/test/java/com/scheduling/architecture/`
  - `ModuleBoundaryTest.java` — Spring Modulith verify 단위 테스트
  - `ArchUnitArchitectureTest.java` — ArchUnit 종합 규칙
  - `NamingConventionTest.java` — 명명 규칙
  - `LayeredArchitectureTest.java` — Layered Architecture
- `backend/app/src/test/java/com/scheduling/architecture/package-info.java`
- `backend/build.gradle.kts` — ArchUnit 테스트 task 전체 빌드에 결합

### 핵심 로직 / 알고리즘

#### A. Spring Modulith Verify (TK-00-2-2의 verify를 표준 JUnit 5 테스트로)

```java
// app/src/test/java/com/scheduling/architecture/ModuleBoundaryTest.java
package com.scheduling.architecture;

import com.scheduling.SchedulingApplication;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;
import org.springframework.modulith.core.ApplicationModules;

class ModuleBoundaryTest {

    static final ApplicationModules MODULES = ApplicationModules.of(SchedulingApplication.class);

    @Test
    @DisplayName("Spring Modulith 모듈 경계 위반이 0건이다")
    void verifyModularStructure() {
        MODULES.verify();
    }

    @Test
    @DisplayName("모든 모듈이 매니페스트에 등재된다 (7개: order·vc·ex·master·audit·notify·common)")
    void allModulesRegistered() {
        long count = MODULES.stream().count();
        org.assertj.core.api.Assertions.assertThat(count).isEqualTo(7);
    }
}
```

#### B. ArchUnit 종합 규칙

```java
// app/src/test/java/com/scheduling/architecture/ArchUnitArchitectureTest.java
package com.scheduling.architecture;

import com.tngtech.archunit.core.domain.JavaClasses;
import com.tngtech.archunit.junit.AnalyzeClasses;
import com.tngtech.archunit.junit.ArchTest;
import com.tngtech.archunit.lang.ArchRule;

import static com.tngtech.archunit.lang.syntax.ArchRuleDefinition.*;
import static com.tngtech.archunit.library.dependencies.SlicesRuleDefinition.slices;

@AnalyzeClasses(packages = "com.scheduling")
class ArchUnitArchitectureTest {

    // ---------- 1. 의존성 그래프 순환 금지 ----------

    @ArchTest
    static final ArchRule no_module_cycles = slices()
        .matching("com.scheduling.(*)..")
        .should().beFreeOfCycles();

    // ---------- 2. internal 패키지는 같은 모듈만 접근 ----------

    @ArchTest
    static final ArchRule internal_packages_only_from_same_module = noClasses()
        .that().resideOutsideOfPackage("com.scheduling.(*)..")
        .or().resideOutsideOfPackages("com.scheduling.common..", "com.scheduling.(*).internal..")
        .should().accessClassesThat()
        .resideInAPackage("..internal..");

    // ---------- 3. common 패키지는 다른 도메인에 의존 금지 (순환 방지) ----------

    @ArchTest
    static final ArchRule common_does_not_depend_on_domain_modules = noClasses()
        .that().resideInAPackage("com.scheduling.common..")
        .should().dependOnClassesThat()
        .resideInAnyPackage(
            "com.scheduling.order..",
            "com.scheduling.vc..",
            "com.scheduling.ex..",
            "com.scheduling.master..",
            "com.scheduling.audit..",
            "com.scheduling.notify.."
        );

    // ---------- 4. 모듈 간 통신은 events 패키지만 ----------

    @ArchTest
    static final ArchRule cross_module_only_via_events_or_api = noClasses()
        .that().resideInAPackage("com.scheduling.(*).internal..")
        .should().dependOnClassesThat()
        .resideInAnyPackage(
            "com.scheduling.(*).internal..",
            "com.scheduling.(*).domain.."
        )
        .andShould().notDependOnClassesThat()
        .resideInTheSamePackageAs("com.scheduling.(*).internal..");

    // (위 규칙은 단순화 — 실제로는 모듈명 기반 정밀 룰 필요. Spring Modulith가 보강)
}
```

#### C. 명명 규칙

```java
// app/src/test/java/com/scheduling/architecture/NamingConventionTest.java
package com.scheduling.architecture;

import com.tngtech.archunit.junit.AnalyzeClasses;
import com.tngtech.archunit.junit.ArchTest;
import com.tngtech.archunit.lang.ArchRule;

import static com.tngtech.archunit.lang.syntax.ArchRuleDefinition.classes;

@AnalyzeClasses(packages = "com.scheduling")
class NamingConventionTest {

    @ArchTest
    static final ArchRule controllers_end_with_Controller = classes()
        .that().areAnnotatedWith("org.springframework.web.bind.annotation.RestController")
        .or().areAnnotatedWith("org.springframework.stereotype.Controller")
        .should().haveSimpleNameEndingWith("Controller");

    @ArchTest
    static final ArchRule services_end_with_Service = classes()
        .that().areAnnotatedWith("org.springframework.stereotype.Service")
        .should().haveSimpleNameEndingWith("Service");

    @ArchTest
    static final ArchRule repositories_end_with_Repository = classes()
        .that().areAnnotatedWith("org.springframework.stereotype.Repository")
        .or().areAssignableTo("org.springframework.data.repository.Repository")
        .should().haveSimpleNameEndingWith("Repository");

    @ArchTest
    static final ArchRule events_end_with_Event = classes()
        .that().resideInAPackage("..events..")
        .should().haveSimpleNameEndingWith("Event");

    @ArchTest
    static final ArchRule listeners_end_with_Listener = classes()
        .that().areAnnotatedWith("org.springframework.modulith.events.ApplicationModuleListener")
        .or().areAnnotatedWith("org.springframework.transaction.event.TransactionalEventListener")
        .should().haveSimpleNameEndingWith("Listener");
}
```

#### D. Layered Architecture (선택, 권장)

```java
// app/src/test/java/com/scheduling/architecture/LayeredArchitectureTest.java
package com.scheduling.architecture;

import com.tngtech.archunit.junit.AnalyzeClasses;
import com.tngtech.archunit.junit.ArchTest;
import com.tngtech.archunit.lang.ArchRule;

import static com.tngtech.archunit.library.Architectures.layeredArchitecture;

@AnalyzeClasses(packages = "com.scheduling")
class LayeredArchitectureTest {

    @ArchTest
    static final ArchRule layers = layeredArchitecture()
        .consideringAllDependencies()
        .layer("Controller").definedBy("..api..", "..controller..")
        .layer("Service").definedBy("..service..", "..internal..")
        .layer("Repository").definedBy("..repository..", "..dao..")
        .layer("Domain").definedBy("..domain..")
        .layer("Events").definedBy("..events..")

        // 의존 방향: Controller → Service → Repository → Domain
        .whereLayer("Controller").mayNotBeAccessedByAnyLayer()
        .whereLayer("Service").mayOnlyBeAccessedByLayers("Controller")
        .whereLayer("Repository").mayOnlyBeAccessedByLayers("Service")
        .whereLayer("Events").mayOnlyBeAccessedByLayers("Service", "Controller");
        // Domain은 모두 접근 가능 (DTO·Enum)
}
```

#### E. Gradle 빌드 결합

```kotlin
// backend/build.gradle.kts (subprojects 블록)
subprojects {
    // ...
    tasks.withType<Test> {
        useJUnitPlatform()
    }
}

// backend/app/build.gradle.kts
dependencies {
    testImplementation(libs.bundles.test.arch.get())   // archunit-junit5 + spring-modulith-test
}

tasks.named<Test>("test") {
    // ArchUnit 테스트는 build > test phase에 포함됨
    // 의도적 위반 발견 시 BUILD FAILED — Sprint 0 DoD 항목 2 달성
}
```

---

## :test_tube: Acceptance Criteria

**1차 검증 방법** (SRS §4.1.6 카탈로그):
- **1차**: T-U (ArchUnit + Spring Modulith JUnit 5 단위 테스트)
- **2차**: A (Analysis — 의존성 그래프 시각화 + 수동 SAD §4 다이어그램 비교)

### 측정 가능 기준
- [ ] `./gradlew :app:test --tests "*architecture*"` 통과
- [ ] `ModuleBoundaryTest` 2 케이스 PASS (verify·7 모듈 등재)
- [ ] `ArchUnitArchitectureTest` 4 규칙 모두 PASS (순환·internal·common·events)
- [ ] `NamingConventionTest` 5 규칙 모두 PASS (Controller·Service·Repository·Event·Listener)
- [ ] `LayeredArchitectureTest` PASS (Controller → Service → Repository 단방향)
- [ ] **의도적 위반 시 즉시 실패**: 예를 들어 `order.api`에서 `vc.internal` 클래스 import → `./gradlew build` 실패 + 명확한 ArchUnit 에러
- [ ] **Sprint 0 DoD 항목 2번 직접 달성** — `ArchUnit + Spring Modulith 통과 / 모듈 경계 위반 0`
- [ ] CI/CD 적용 가능 (Jenkins step 예시 1개 작성)

### 회귀 시나리오 (T-U)
- **케이스 1 (정상)**: 모든 모듈 정합 → `./gradlew :app:test` BUILD SUCCESSFUL
- **케이스 2 (모듈 경계 위반)**: `order/internal/X.java`에서 `vc.internal.Y` import 추가 → ArchUnitArchitectureTest.no_module_cycles 또는 internal_packages_only_from_same_module 실패
- **케이스 3 (순환 위반)**: `common`에서 `order` 클래스 import 추가 → common_does_not_depend_on_domain_modules 실패
- **케이스 4 (명명 위반)**: `@RestController` 클래스 이름이 `Controller`로 안 끝남 → NamingConventionTest 실패
- **케이스 5 (Layered 위반)**: Repository가 Controller import → LayeredArchitectureTest 실패

---

## :checkered_flag: Definition of Done
- [ ] 위 모든 측정 기준 통과
- [ ] 4 테스트 클래스 작성 + 모두 GREEN (ModuleBoundary·ArchUnit·Naming·Layered)
- [ ] **Sprint 0 DoD 항목 2번 직접 달성** — `make build` 또는 `./gradlew build` 단계에서 모듈 경계 위반 0건
- [ ] CI/CD 파이프라인 통합 가능 (EP-32 ST-32-1 입력 — Jenkins step `./gradlew :app:test`)
- [ ] 코드 리뷰 1명 이상 승인 (백엔드 리드 + QA)
- [ ] NFR 매핑: REQ-NF-COM-004 (API 전방 호환성 회귀 방지)
- [ ] Sprint Review 데모:
  - 정상 빌드 (BUILD SUCCESSFUL)
  - 의도적 위반 시 BUILD FAILED + 명확한 ArchUnit 에러 메시지 시연
  - Modulith docs PlantUML로 시각화 (선택)

---

## :construction: Dependencies
- **선행 (Depends on)**:
  - 같은 Story 내: [TK-00-2-1](TK-00-2-1.md) (Gradle + ArchUnit BOM), [TK-00-2-2](TK-00-2-2.md) (Modulith 모듈 정의)
- **후행 (Blocks)**:
  - EP-32 ST-32-1 (CI/CD — 본 테스트를 Jenkins quality gate로 적용)
  - 모든 Sprint 1~5 백엔드 Epic — 본 Task의 테스트가 매 PR/merge 시 회귀 방지
- **Critical Path 여부**: ⭐ **Critical Path 속함** (Sprint 0 DoD 항목 2 직접 달성)
- **Phase B 종속**: 해당 없음

---

## :shield: Risk Mitigation
- **완화 SRS-RSK / SAD-RSK**:
  - SAD §11 SAD-RSK-001 (Java/Spring 풀 채용 어려움) — ArchUnit이 신규 개발자의 의도치 않은 모듈 경계 위반을 빌드 단계에서 차단 → 학습 곡선 보조
- **잔여 위험·전제**:
  - 전제 1: 본 Task는 Phase 0 시점에서 Sprint 1~5 코드가 아직 없으므로 "예방적" 테스트 — 실제 위반은 Sprint 진행 중 점진 검출. CI/CD에 결합 필수
  - 전제 2: ArchUnit 룰이 너무 엄격하면 false positive 가능 — 개발자 피드백 1주 후 룰 조정 권장
  - 잔여 위험: Spring Modulith 1.3+ 업그레이드 시 ApplicationModules API 변경 가능 — Version Catalog 단일 변경으로 처리

---

## :memo: Implementation Notes
- **기술 힌트**:
  - `@AnalyzeClasses(packages = "...")` → 클래스 로딩 최적화 (테스트 클래스 제외 자동)
  - ArchUnit 5.x는 JUnit 5 통합 — `@ArchTest static final ArchRule ...` 패턴
  - Spring Modulith `ApplicationModules.verify()`와 ArchUnit은 **상호 보완** — Modulith는 런타임 정확도, ArchUnit은 빌드 강제력
- **유사 패턴**:
  - 향후 모듈 추가 시 (Phase 2 MRP) 본 테스트 자동 적용 — 새 모듈도 동일 룰 통과 필수
- **표준 참조**:
  - ArchUnit Docs: https://www.archunit.org/userguide/html/000_Index.html
  - ArchUnit JUnit 5: https://github.com/TNG/ArchUnit/tree/main/archunit-junit5
  - Spring Modulith Verification: https://docs.spring.io/spring-modulith/reference/verification.html
- **참고 외부 자료**:
  - "ArchUnit으로 아키텍처 강제" 한국어 자료 — Phase 1.0 출시 후 신규 개발자 온보딩 자료로 활용
