---
name: Backend+Frontend Task (KST Enforcement)
about: 0.4 PD
title: "[TK-34-3-1] 모든 timestamp KST (Spring·DB·UI)"
labels: 'sprint:S2, epic:EP-34, story:ST-34-3, type:backend+frontend, priority:must, owner:backend+frontend, br:X04'
---

## :dart: Task Summary
- 추정: 0.4 PD / Owner: Backend + Frontend
- 3개 layer에서 KST 강제 — Spring (`spring.jackson.time-zone=Asia/Seoul`), PostgreSQL (`timezone='Asia/Seoul'`), Frontend (`dayjs` + Intl).

## :hammer_and_wrench: Implementation Plan

```
backend/scheduling-engine/src/main/resources/
  application.yml                                 [수정 — timezone]
backend/scheduling-engine/src/main/java/com/scheduling/config/
  TimeZoneConfig.java                             [Bean — TimeZone.setDefault]
infra/postgres/
  postgresql.conf                                 [timezone='Asia/Seoul']
frontend/src/utils/
  date.ts                                          [dayjs ko + Asia/Seoul]
```

### `application.yml`

```yaml
spring:
  jackson:
    time-zone: Asia/Seoul
    date-format: yyyy-MM-dd'T'HH:mm:ss
  jpa:
    properties:
      hibernate.jdbc.time_zone: Asia/Seoul
```

### `TimeZoneConfig.java`

```java
@Configuration
public class TimeZoneConfig {
    @PostConstruct
    public void init() {
        TimeZone.setDefault(TimeZone.getTimeZone("Asia/Seoul"));
        System.setProperty("user.timezone", "Asia/Seoul");
    }
}
```

### `date.ts`

```typescript
import dayjs from 'dayjs';
import 'dayjs/locale/ko';
import timezone from 'dayjs/plugin/timezone';
import utc from 'dayjs/plugin/utc';

dayjs.extend(utc);
dayjs.extend(timezone);
dayjs.tz.setDefault('Asia/Seoul');
dayjs.locale('ko');

export function formatKst(d: string | Date): string {
  return dayjs(d).tz('Asia/Seoul').format('YYYY-MM-DD HH:mm:ss');
}
```

## :test_tube: Acceptance Criteria
- [ ] Spring 모든 timestamp KST
- [ ] PostgreSQL `SHOW timezone;` = 'Asia/Seoul'
- [ ] Frontend 시간 표시 KST
- [ ] JSON serialize/deserialize KST 일관

## :checkered_flag: DoD
- [ ] Config 3 layer + 검증
- [ ] 코드 리뷰

## :construction: Dependencies
- 선행: [TK-00-2-1](../../EP-00/ST-00-2/TK-00-2-1.md), [TK-00-3-1](../../EP-00/ST-00-3/TK-00-3-1.md)
- 후행: [TK-34-3-2](TK-34-3-2.md)
- Critical Path: ⭐⭐
