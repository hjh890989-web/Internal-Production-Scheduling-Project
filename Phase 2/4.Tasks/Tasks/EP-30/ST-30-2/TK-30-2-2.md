---
name: Backend Task (RBAC Matrix + Spring Security Config)
about: 0.8 PD
title: "[TK-30-2-2] RBAC 매트릭스 + Spring Security Config"
labels: 'sprint:S1, epic:EP-30, story:ST-30-2, type:backend, priority:must, owner:backend, sec:RBAC'
---

## :dart: Task Summary
- 추정: 0.8 PD / Owner: Backend
- `SecurityConfig.java` — JWT resource server + @EnableMethodSecurity. RBAC 매트릭스 문서화. ArchUnit 규칙 — 모든 controller method `@PreAuthorize` 강제.

## :link: References
- WBS §10 EP-30 ST-30-2 (Task 2)
- SRS REQ-NF-SEC-003
- 연관: 선행 [TK-30-2-1](TK-30-2-1.md), [TK-42-3-1](../../EP-42/ST-42-3/TK-42-3-1.md) (Phase 4 통합), 후행 [TK-30-2-3](TK-30-2-3.md)

## :hammer_and_wrench: Implementation Plan

```
backend/scheduling-engine/src/main/java/com/scheduling/security/
  SecurityConfig.java                            [신규]
docs/security/
  rbac-matrix.md                                 [전체 매트릭스]
backend/scheduling-engine/src/test/java/com/scheduling/architecture/
  PreAuthorizeArchTest.java                      [ArchUnit]
```

### `SecurityConfig.java`

```java
@Configuration @EnableWebSecurity @EnableMethodSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final KeycloakJwtAuthConverter jwtConverter;

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/actuator/health/**", "/actuator/prometheus").permitAll()
                .requestMatchers("/api/v1/public/**").permitAll()
                .requestMatchers("/swagger-ui/**", "/v3/api-docs/**").permitAll()
                .anyRequest().authenticated())
            .oauth2ResourceServer(oauth2 -> oauth2
                .jwt(jwt -> jwt.jwtAuthenticationConverter(jwtConverter))
                .accessDeniedHandler(new CustomAccessDeniedHandler())
                .authenticationEntryPoint(new CustomAuthenticationEntryPoint()))
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS));
        return http.build();
    }
}
```

### `docs/security/rbac-matrix.md`

```markdown
# RBAC 매트릭스 (REQ-NF-SEC-003)

## 4 Role

| Role | 설명 | 사용자 |
|------|------|--------|
| PLANNER | 생산계획팀 | 스케줄 작성·확정·override |
| STK_USER | 현장 STK 작업자 | 시뮬뷰 조회·제안 |
| IT_OPS | IT 운영팀 | 마스터·시스템 관리 |
| READ_ONLY | 감사·임원·READ_ONLY | 조회만 |

## API 매트릭스 (sample)

| Endpoint | PLANNER | STK_USER | IT_OPS | READ_ONLY |
|----------|:-------:|:--------:|:------:|:---------:|
| `GET /api/v1/schedule/vc` | ✓ | ✓ | ✓ | ✓ |
| `POST /api/v1/schedule/vc/{id}/confirm` | ✓ | ✗ | ✗ | ✗ |
| `POST /api/v1/schedule/vc/{id}/override-intraday-lock` | ✓ | ✗ | ✗ | ✗ |
| `POST /api/v1/schedule/vc/proposals` (제안 생성) | ✓ | ✓ | ✗ | ✗ |
| `POST /api/v1/schedule/vc/proposals/{id}/accept` | ✓ | ✗ | ✗ | ✗ |
| `POST /api/v1/master/holiday` | ✗ | ✗ | ✓ | ✗ |
| `GET /api/v1/audit/search` | ✓ | ✗ | ✓ | ✓ |
| `POST /api/v1/master/restore` | ✗ | ✗ | ✓ | ✗ |
| ... (전체 매트릭스) | | | | |
```

### `PreAuthorizeArchTest.java`

```java
class PreAuthorizeArchTest {

    private static final JavaClasses CLASSES =
        new ClassFileImporter().importPackages("com.scheduling..");

    @Test
    void all_controller_methods_must_have_pre_authorize() {
        methods().that().areDeclaredInClassesThat().areAnnotatedWith(RestController.class)
            .and().arePublic()
            .and().areNotDeclaredIn(BaseController.class)        // health 등 예외
            .should().beAnnotatedWith(PreAuthorize.class)
            .check(CLASSES);
    }
}
```

## :test_tube: Acceptance Criteria

- [ ] Spring Security 6.x JWT resource server
- [ ] @EnableMethodSecurity
- [ ] anonymous endpoint 4종 (health·public·swagger)
- [ ] RBAC 매트릭스 문서 작성 (20+ endpoint)
- [ ] ArchUnit 규칙 PASS — 모든 controller method @PreAuthorize
- [ ] Negative 테스트 — 권한 없는 access 403

## :checkered_flag: Definition of Done

- [ ] Config + 매트릭스 + ArchUnit
- [ ] Testcontainers 통합 테스트
- [ ] 커버리지 ≥ 80%
- [ ] 보안팀 sign-off
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-30-2-1](TK-30-2-1.md)
- 후행: [TK-30-2-3](TK-30-2-3.md), [TK-42-3-1](../../EP-42/ST-42-3/TK-42-3-1.md)
- Critical Path: ⭐⭐

## :shield: Risk Mitigation

- ArchUnit 규칙 — 컴파일 시점 차단 — Phase 3 신규 controller 추가 시 자동 강제
- 매트릭스 운영팀 검토 후 sign-off — 실 운영 정합성

## :memo: Implementation Notes

- SessionCreationPolicy.STATELESS — JWT 기반 무상태 (CSRF disable OK)
- BaseController 패턴 — Actuator 등 예외 처리 위한 marker
- Method-level + URL-level 이중 보호 (defense in depth)
