---
name: Backend Task (4 Role Mapping)
about: 0.7 PD
title: "[TK-30-2-1] 4 role 매핑 (PLANNER·STK·IT_OPS·READ_ONLY)"
labels: 'sprint:S1, epic:EP-30, story:ST-30-2, type:backend, priority:must, owner:backend, sec:RBAC'
---

## :dart: Task Summary
- 추정: 0.7 PD / Owner: Backend
- Keycloak realm role 4종 → Spring `GrantedAuthority` 매핑. JWT claim `realm_access.roles` → `ROLE_PLANNER`·`ROLE_STK_USER`·`ROLE_IT_OPS`·`ROLE_READ_ONLY`.

## :link: References
- WBS §10 EP-30 ST-30-2 (Task 1)
- SRS REQ-NF-SEC-003
- 연관: 선행 [TK-30-1-1](../ST-30-1/TK-30-1-1.md), 후행 [TK-30-2-2](TK-30-2-2.md)

## :hammer_and_wrench: Implementation Plan

```
backend/scheduling-engine/src/main/java/com/scheduling/security/
  KeycloakJwtAuthConverter.java                  [신규]
  RoleConstants.java                             [상수]
```

### `KeycloakJwtAuthConverter.java`

```java
@Component
public class KeycloakJwtAuthConverter implements Converter<Jwt, AbstractAuthenticationToken> {

    @Override
    public AbstractAuthenticationToken convert(Jwt jwt) {
        Collection<GrantedAuthority> authorities = extractAuthorities(jwt);
        return new JwtAuthenticationToken(jwt, authorities, getPrincipalName(jwt));
    }

    private Collection<GrantedAuthority> extractAuthorities(Jwt jwt) {
        Map<String, Object> realmAccess = jwt.getClaimAsMap("realm_access");
        if (realmAccess == null) return Set.of();

        @SuppressWarnings("unchecked")
        Collection<String> roles = (Collection<String>) realmAccess.getOrDefault("roles", Set.of());
        return roles.stream()
            .filter(role -> RoleConstants.VALID_ROLES.contains(role))
            .map(role -> "ROLE_" + role)
            .map(SimpleGrantedAuthority::new)
            .collect(Collectors.toSet());
    }

    private String getPrincipalName(Jwt jwt) {
        return jwt.getClaimAsString("preferred_username");
    }
}
```

### `RoleConstants.java`

```java
public final class RoleConstants {
    public static final String PLANNER = "PLANNER";
    public static final String STK_USER = "STK_USER";
    public static final String IT_OPS = "IT_OPS";
    public static final String READ_ONLY = "READ_ONLY";

    public static final Set<String> VALID_ROLES = Set.of(PLANNER, STK_USER, IT_OPS, READ_ONLY);

    private RoleConstants() {}
}
```

## :test_tube: Acceptance Criteria

- [ ] **JWT realm_access.roles** → `GrantedAuthority` 변환
- [ ] **4 role 모두** `ROLE_` prefix 자동
- [ ] **알 수 없는 role** → 무시 (보안 측면)
- [ ] **principal name** = preferred_username
- [ ] 단위 테스트 ≥ 5 케이스 (각 role + 잘못된 토큰)

## :checkered_flag: Definition of Done

- [ ] Converter + Constants
- [ ] 단위 테스트 ≥ 5
- [ ] 커버리지 ≥ 80%
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-30-1-1](../ST-30-1/TK-30-1-1.md), [TK-30-1-2](../ST-30-1/TK-30-1-2.md)
- 후행: [TK-30-2-2](TK-30-2-2.md), 모든 controller (PLANNER·STK 분리 적용)
- Critical Path: ⭐⭐

## :shield: Risk Mitigation

- 알 수 없는 role 무시 — 사고로 신규 role 추가 시에도 escalation 방지
- preferred_username 충돌 — Keycloak realm 설정으로 강제 unique

## :memo: Implementation Notes

- Spring Security 6.x — `JwtAuthenticationConverter` 표준
- VALID_ROLES Set 사용 — 룰북에 명시된 4 role만 인정 (whitelist)
- 향후 권한 세분화 (예: PLANNER_ADMIN·PLANNER_VIEWER) 시 enum으로 확장
