---
name: Backend Task (403/401 Handler + ProblemDetail)
about: 0.6 PD
title: "[TK-30-2-3] 403/401 처리 + ProblemDetail"
labels: 'sprint:S1, epic:EP-30, story:ST-30-2, type:backend, priority:must, owner:backend, sec:RBAC'
---

## :dart: Task Summary
- 추정: 0.6 PD / Owner: Backend
- RFC 7807 ProblemDetail — 한국어 에러 메시지 + redirect URL. 401 → login redirect, 403 → "권한 없음" 모달.

## :link: References
- WBS §10 EP-30 ST-30-2 (Task 3)
- SRS REQ-NF-SEC-003, REQ-NF-USA-003 (한국어)
- 연관: 선행 [TK-30-2-2](TK-30-2-2.md), 후행 모든 controller

## :hammer_and_wrench: Implementation Plan

```
backend/scheduling-engine/src/main/java/com/scheduling/security/
  CustomAccessDeniedHandler.java                 [403]
  CustomAuthenticationEntryPoint.java            [401]
  ErrorMessages.properties                       [한국어]
```

### `CustomAccessDeniedHandler.java`

```java
@Component @Slf4j
public class CustomAccessDeniedHandler implements AccessDeniedHandler {

    private final ObjectMapper mapper;

    @Override
    public void handle(HttpServletRequest request, HttpServletResponse response,
                       AccessDeniedException e) throws IOException {
        log.warn("403 Access denied: user={}, uri={}",
            getUserId(request), request.getRequestURI());

        var problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.FORBIDDEN,
            "권한이 없습니다 — 필요 role 검토 필요");
        problem.setTitle("Access Denied");
        problem.setProperty("requiredRoles", extractRequiredRoles(request));
        problem.setProperty("currentRoles", getCurrentRoles());
        problem.setInstance(URI.create(request.getRequestURI()));

        response.setStatus(HttpStatus.FORBIDDEN.value());
        response.setContentType(MediaType.APPLICATION_PROBLEM_JSON_VALUE);
        mapper.writeValue(response.getOutputStream(), problem);
    }
}
```

### `CustomAuthenticationEntryPoint.java`

```java
@Component
public class CustomAuthenticationEntryPoint implements AuthenticationEntryPoint {

    private final ObjectMapper mapper;

    @Override
    public void commence(HttpServletRequest request, HttpServletResponse response,
                          AuthenticationException e) throws IOException {
        var problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.UNAUTHORIZED,
            "인증이 필요합니다 — 로그인 후 다시 시도하세요");
        problem.setTitle("Authentication Required");
        problem.setProperty("loginUrl", "/login");

        response.setStatus(HttpStatus.UNAUTHORIZED.value());
        response.setContentType(MediaType.APPLICATION_PROBLEM_JSON_VALUE);
        mapper.writeValue(response.getOutputStream(), problem);
    }
}
```

## :test_tube: Acceptance Criteria

- [ ] **401 Unauthorized** — 인증 없는 access (ProblemDetail + 한국어)
- [ ] **403 Forbidden** — 권한 부족 (requiredRoles·currentRoles 표시)
- [ ] **ProblemDetail RFC 7807** 형식 준수
- [ ] **로그 — actor·URI·required role** 기록
- [ ] 단위 + 통합 테스트 ≥ 5 케이스

## :checkered_flag: Definition of Done

- [ ] Handler 2종 + 메시지 properties
- [ ] Testcontainers 통합 테스트
- [ ] 커버리지 ≥ 80%
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-30-2-2](TK-30-2-2.md)
- 후행: 모든 controller (Sprint 1~5)
- Critical Path: ⭐⭐

## :shield: Risk Mitigation

- 에러 메시지에 민감 정보 노출 차단 — currentRoles는 사용자 자신 role만
- 로그 actor·URI — 분기 보안 audit 사용

## :memo: Implementation Notes

- RFC 7807 ProblemDetail — Spring 6+ 내장 (additional library 불필요)
- 한국어 메시지 — i18n key 사용 (EP-43 ST-43-3과 정합)
- 향후 fine-grained permission (read/write/delete 분리) 검토
