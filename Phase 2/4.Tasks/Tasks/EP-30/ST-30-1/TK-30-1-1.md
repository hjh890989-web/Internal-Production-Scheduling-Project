---
name: DevOps Task (Keycloak Container)
about: 1.0 PD
title: "[TK-30-1-1] Keycloak 24 컨테이너 (Docker Compose 통합)"
labels: 'sprint:S0, epic:EP-30, story:ST-30-1, type:infra, priority:must, owner:devops, sec:SSO'
---

## :dart: Task Summary
- 추정: 1.0 PD / Owner: DevOps
- Keycloak 24 LTS 공식 이미지 + Docker Compose 통합. PostgreSQL 16 db 분리 (keycloak own DB). realm `scheduling-system` 사전 생성 (realm-export.json).

## :link: References
- WBS §10 EP-30 ST-30-1 (Task 1)
- SAD ADR-012
- 연관: 선행 [TK-00-1-1](../../EP-00/ST-00-1/TK-00-1-1.md), 후행 [TK-30-1-2](TK-30-1-2.md)

## :hammer_and_wrench: Implementation Plan

```
infra/docker-compose.yml                          [수정 — keycloak service 추가]
infra/keycloak/
  realm-scheduling-system.json                    [실 사용 realm export]
  themes/                                         [커스텀 한국어 테마 — 선택]
```

### `docker-compose.yml` keycloak service

```yaml
keycloak:
  image: quay.io/keycloak/keycloak:24.0
  command: start
  environment:
    KC_DB: postgres
    KC_DB_URL: jdbc:postgresql://keycloak-db:5432/keycloak
    KC_DB_USERNAME: keycloak
    KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
    KC_HOSTNAME: ${KEYCLOAK_HOST}
    KC_HTTP_ENABLED: true
    KC_PROXY: edge
    KEYCLOAK_ADMIN: admin
    KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
  volumes:
    - ./keycloak/realm-scheduling-system.json:/opt/keycloak/data/import/realm.json:ro
  depends_on: [keycloak-db]
  ports: ["8080:8080"]
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8080/health/ready"]
    interval: 30s

keycloak-db:
  image: postgres:16-alpine
  environment:
    POSTGRES_DB: keycloak
    POSTGRES_USER: keycloak
    POSTGRES_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
  volumes:
    - keycloak-db-data:/var/lib/postgresql/data
```

### `realm-scheduling-system.json` (sketch)

```json
{
  "realm": "scheduling-system",
  "enabled": true,
  "clients": [{
    "clientId": "scheduling-app",
    "publicClient": false,
    "directAccessGrantsEnabled": true,
    "standardFlowEnabled": true,
    "redirectUris": ["https://scheduling.internal/*"]
  }],
  "roles": {
    "realm": [
      { "name": "PLANNER" },
      { "name": "STK_USER" },
      { "name": "IT_OPS" },
      { "name": "READ_ONLY" }
    ]
  },
  "passwordPolicy": "length(12) and digits(1) and upperCase(1) and specialChars(1)",
  "bruteForceProtected": true,
  "failureFactor": 5
}
```

---

## :test_tube: Acceptance Criteria

- [ ] Keycloak 24 LTS 컨테이너 booting (≤ 30초)
- [ ] PostgreSQL 16 keycloak DB 분리 (운영 DB와 별개)
- [ ] realm `scheduling-system` 자동 import (4 role 포함)
- [ ] admin 계정 첫 로그인 (`/admin`)
- [ ] `/health/ready` 200 OK
- [ ] Compose `up -d` → 전체 환경 정상

## :checkered_flag: Definition of Done

- [ ] Compose service + realm config + healthcheck
- [ ] Testcontainers 통합 테스트
- [ ] 운영 가이드 (`docs/operations/keycloak-setup.md`)
- [ ] 코드 리뷰 1명 이상 승인

## :construction: Dependencies

- 선행: [TK-00-1-1](../../EP-00/ST-00-1/TK-00-1-1.md)
- 후행: [TK-30-1-2](TK-30-1-2.md), [TK-30-1-3](TK-30-1-3.md), [TK-30-2-1](../ST-30-2/TK-30-2-1.md)
- Critical Path: ⭐⭐ (Phase 3 진입 게이트)

## :shield: Risk Mitigation

- Keycloak DB 손상 — pg_basebackup 별도 (EP-41 ST-41-4 patten 재사용)
- realm-export.json git tracking — 환경별 변수 분리 (KC_HOSTNAME)

## :memo: Implementation Notes

- Keycloak 24 LTS — 2027년까지 지원
- `start` 모드 (production) — `start-dev` 금지 (TLS 강제)
- 향후 cluster 모드 (다중 인스턴스) 검토 — Phase 2+
