---
name: DevOps Task (Env Variables + Secrets)
about: 0.6 PD
title: "[TK-33-1-3] 환경별 변수 분리 + secrets 관리"
labels: 'sprint:S0, epic:EP-33, story:ST-33-1, type:infra, priority:must, owner:devops+security, deploy:env'
---

## :dart: Task Summary
- 추정: 0.6 PD / Owner: DevOps + Security
- `.env.dev`·`.env.stg`·`.env.prod` — Docker Compose env_file 활용. Secrets (DB·Keycloak·Slack webhook 등) Vault 또는 Docker Secrets 검토. .gitignore 강제.

## :link: References
- WBS §10 EP-33 ST-33-1 (Task 3)
- SRS REQ-NF-SEC-005 (DLP)
- 연관: 선행 [TK-33-1-1](TK-33-1-1.md), [TK-33-1-2](TK-33-1-2.md)

## :hammer_and_wrench: Implementation Plan

```
infra/
  .env.dev.example
  .env.stg.example
  .env.prod.example                              [실 .env.prod는 .gitignore]
  .gitignore                                     [수정 — .env.* 추가]
docs/operations/
  secrets-management.md
```

### `.env.prod.example`

```bash
# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=scheduling_prod
DB_USERNAME=app_user
DB_PASSWORD=<REPLACE>

# Keycloak
KEYCLOAK_HOST=keycloak.scheduling.internal
KEYCLOAK_ADMIN=<REPLACE>
KEYCLOAK_ADMIN_PASSWORD=<REPLACE>
KEYCLOAK_DB_PASSWORD=<REPLACE>

# Backup
BACKUP_BUCKET=s3://internal-backup/scheduling
BACKUP_ENCRYPTION_KEY=<REPLACE>

# Slack
SLACK_WEBHOOK_URL=<REPLACE>

# Sentry
SENTRY_DSN=<REPLACE>
```

### `.gitignore` 추가

```
# Secrets — never commit
.env
.env.dev
.env.stg
.env.prod
*.env
!*.env.example
```

### `secrets-management.md`

```markdown
# Secrets 관리 정책

## 원칙
- `.env.*` (example 제외) git 추적 금지
- Production secret은 봉인 보관 (사용 시 IT lead 결재)
- 분기별 rotation (KEYCLOAK_ADMIN_PASSWORD, DB_PASSWORD)

## Vault 통합 (Phase 2+)
- Vault Agent — Compose secrets API integration
- 현재 Phase 1: .env 파일 + 파일 시스템 권한 (chmod 600)
```

## :test_tube: Acceptance Criteria

- [ ] 3개 .env.example (dev·stg·prod)
- [ ] .env.* git 추적 차단 검증
- [ ] secret 파일 권한 600 (Linux)
- [ ] 운영 가이드

## :checkered_flag: Definition of Done

- [ ] .env + gitignore + 가이드
- [ ] 보안팀 sign-off
- [ ] 코드 리뷰

## :construction: Dependencies

- 선행: [TK-33-1-1](TK-33-1-1.md), [TK-33-1-2](TK-33-1-2.md)
- 후행: [TK-33-2-1](../ST-33-2/TK-33-2-1.md)
- Critical Path: ⭐⭐

## :memo: Implementation Notes

- 현재는 .env 파일 — Vault 통합은 Phase 2+ 검토
- chmod 600 — Compose host에서 권한 강제
- Phase 2+ Doppler / 1Password Connect 검토
