---
name: Backend Task (Formula Preservation)
about: 단일 작업 단위 — 0.7 PD
title: "[TK-12-1-2] 수식 보존 (formula·cellType·styling)"
labels: 'sprint:S4, epic:EP-12, story:ST-12-1, type:backend, priority:must, owner:backend'
assignees: ''
---

## :dart: Task Summary
- **Task ID**: TK-12-1-2
- **소속**: EP-12 / ST-12-1 / Sprint S4
- **우선순위**: Must / **추정**: 0.7 PD (~5.6h) / **Owner**: Backend
- **작업 요약**: 원본 template.xlsx의 수식 (`=A1+B1`·`=SUM(C2:C10)`) 그대로 보존. cell type (FORMULA·NUMERIC·STRING) 정확 유지. styling (border·color·font) 유지. `XSSFFormulaEvaluator.evaluateAllFormulaCells()` 호출로 cached value 동기화 — Excel 열 때 즉시 표시.

---

## :link: References
- **상위 Story**: [`_Story_Overview.md`](_Story_Overview.md)
- **WBS**: §7 EP-12 ST-12-1 (Task 2)
- **연관**: 선행 [TK-12-1-1](TK-12-1-1.md), 후행 [TK-12-1-3](TK-12-1-3.md)

---

## :hammer_and_wrench: Implementation Plan

```
backend/scheduling-engine/
  src/main/java/com/scheduling/export/excel/
    MasterExcelExporter.java                     [수정 — setCell formula 분기]
    CellTypePreserver.java                       [신규 helper]
```

### `CellTypePreserver.java`

```java
@Component
public class CellTypePreserver {

    /**
     * 원본 cell type 보존 + 값 갱신.
     *  - FORMULA: 수식 그대로 (값만 갱신 시 cell.setCellFormula 호출 안 함)
     *  - NUMERIC: setCellValue(double)
     *  - STRING: setCellValue(String)
     *  - BOOLEAN: setCellValue(boolean)
     *  - styling은 cell.setCellStyle(원본) — POI 자동
     */
    public void setValue(Cell cell, Object value) {
        if (cell.getCellType() == CellType.FORMULA) {
            return;     // 수식 셀은 갱신 안 함 (cached value는 evaluator가 처리)
        }

        if (value == null) {
            cell.setBlank();
            return;
        }

        switch (value) {
            case String s -> cell.setCellValue(s);
            case Integer i -> cell.setCellValue(i);
            case Long l -> cell.setCellValue(l);
            case Double d -> cell.setCellValue(d);
            case BigDecimal bd -> cell.setCellValue(bd.doubleValue());
            case Boolean b -> cell.setCellValue(b);
            case LocalDate ld -> cell.setCellValue(ld);
            case LocalDateTime ldt -> cell.setCellValue(ldt);
            default -> cell.setCellValue(value.toString());
        }
    }
}
```

### `MasterExcelExporter` 수정

```java
@Service @RequiredArgsConstructor
public class MasterExcelExporter {

    private final CellTypePreserver cellPreserver;
    // ...

    public byte[] export() {
        try (XSSFWorkbook workbook = templateLoader.load("master-template.xlsx");
             ByteArrayOutputStream baos = new ByteArrayOutputStream()) {

            writeVcConstraintSheet(workbook.getSheet("성형공정_제약조건"));
            // ... 기타 시트

            // 핵심: 수식 cached value 동기화
            //   1. 모든 수식 cell을 재계산
            //   2. Excel이 파일 열 때 재계산 안 해도 정확한 값 표시
            //   3. "재계산이 필요합니다" 알림 없음
            XSSFFormulaEvaluator evaluator = workbook.getCreationHelper().createFormulaEvaluator();
            evaluator.evaluateAll();

            workbook.write(baos);
            return baos.toByteArray();
        } catch (IOException e) {
            throw new ExportException("master export 실패", e);
        }
    }

    private void writeVcConstraintSheet(Sheet sheet) {
        var data = vcRepo.findAll();
        int rowIdx = 1;
        for (var vc : data) {
            Row row = sheet.getRow(rowIdx);
            if (row == null) row = sheet.createRow(rowIdx);

            cellPreserver.setValue(row.getCell(0, MissingCellPolicy.CREATE_NULL_AS_BLANK), vc.getHoseId());
            cellPreserver.setValue(row.getCell(1, MissingCellPolicy.CREATE_NULL_AS_BLANK), vc.getMachineId());
            cellPreserver.setValue(row.getCell(10, MissingCellPolicy.CREATE_NULL_AS_BLANK), vc.getLpLeftSetting());
            cellPreserver.setValue(row.getCell(11, MissingCellPolicy.CREATE_NULL_AS_BLANK), vc.getLpRightSetting());

            // 11열 (수식 셀 가정): cellPreserver가 자동 skip
            rowIdx++;
        }
    }
}
```

### 검증 helper `ExcelDiff.java`

```java
public class ExcelDiff {

    /**
     * 두 워크북 간 cell-수준 차이 비율 계산.
     */
    public static double diffRatio(XSSFWorkbook original, XSSFWorkbook exported) {
        long totalCells = 0, differentCells = 0;

        for (int s = 0; s < original.getNumberOfSheets(); s++) {
            Sheet origSheet = original.getSheetAt(s);
            Sheet expSheet = exported.getSheet(origSheet.getSheetName());
            if (expSheet == null) continue;

            for (Row origRow : origSheet) {
                Row expRow = expSheet.getRow(origRow.getRowNum());
                if (expRow == null) {
                    totalCells += origRow.getLastCellNum();
                    differentCells += origRow.getLastCellNum();
                    continue;
                }
                for (Cell origCell : origRow) {
                    totalCells++;
                    Cell expCell = expRow.getCell(origCell.getColumnIndex());
                    if (!cellEquals(origCell, expCell)) differentCells++;
                }
            }
        }

        return totalCells == 0 ? 0.0 : (double) differentCells / totalCells;
    }

    private static boolean cellEquals(Cell a, Cell b) {
        if (a == null || b == null) return a == b;
        if (a.getCellType() != b.getCellType()) return false;
        return switch (a.getCellType()) {
            case FORMULA -> a.getCellFormula().equals(b.getCellFormula());
            case NUMERIC -> Double.compare(a.getNumericCellValue(), b.getNumericCellValue()) == 0;
            case STRING -> a.getStringCellValue().equals(b.getStringCellValue());
            case BOOLEAN -> a.getBooleanCellValue() == b.getBooleanCellValue();
            case BLANK -> true;
            default -> false;
        };
    }
}
```

---

## :test_tube: Acceptance Criteria

**검증**: T-U + T-I

- [ ] **FORMULA cell** — `cellPreserver.setValue()` 호출해도 수식 변경 안 됨
- [ ] **NUMERIC·STRING·BOOLEAN·DATE** 모두 정확 type 유지
- [ ] **`evaluator.evaluateAll()`** 호출 — cached value 동기화
- [ ] **styling 유지** — border·color·font 원본 동일 (POI 자동, 검증 fixture)
- [ ] **`ExcelDiff.diffRatio()`** — template ↔ 변경 없이 export 비교 시 < 2%
- [ ] 단위 + 통합 테스트 ≥ 5 케이스

---

## :checkered_flag: Definition of Done

- [ ] 위 측정 기준 통과
- [ ] CellTypePreserver + Exporter 수정 + ExcelDiff helper
- [ ] Testcontainers 통합 테스트
- [ ] 커버리지 ≥ 80%
- [ ] 코드 리뷰 1명 이상 승인

---

## :construction: Dependencies

- **선행**: [TK-12-1-1](TK-12-1-1.md)
- **후행**: [TK-12-1-3](TK-12-1-3.md)
- **Critical Path**: ⭐

---

## :shield: Risk Mitigation

- POI evaluator overhead — 대용량 시 SXSSF + manual evaluate 검토
- DATE type — POI 기본 NUMERIC + date style (Excel 동작 — 본 패턴 안전)

---

## :memo: Implementation Notes

- `cell.setCellType(FORMULA)`는 deprecated — POI 5.x는 setCellFormula 자체로 type 결정
- Java 17 sealed pattern matching (`case String s -> ...`) 활용 — POI legacy 컬렉션 분기 가독성
- evaluateAll은 cycle 검출 (순환 수식 → exception) — fail-fast로 마스터 정합성 검증
