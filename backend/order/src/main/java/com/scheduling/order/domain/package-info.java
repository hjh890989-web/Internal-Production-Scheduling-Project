/**
 * order 모듈 — 도메인 (Order entity·OrderDraft·OrderType).
 *
 * <p>본 패키지는 모듈 내부 — 외부 모듈에서 직접 접근 금지.
 * 외부 노출은 {@link com.scheduling.order.api} (NamedInterface "api") 만 사용.
 * NamedInterface annotation 미사용 — Spring Modulith 가 internal 로 처리.
 */
package com.scheduling.order.domain;
