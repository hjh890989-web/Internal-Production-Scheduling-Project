package com.scheduling.master.calendar;

/** master.holiday 분류 — TK-06-1-1. */
public enum HolidayType {
    /** 법정공휴일 (국가 지정). */
    LEGAL,
    /** 사내 휴일 (단체협약·창립기념일 등). */
    COMPANY,
    /** 정비일 (가류기 점검·설비 정지). */
    MAINTENANCE
}
